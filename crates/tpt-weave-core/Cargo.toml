[package]
name = "tpt-weave-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Core repository model types, token accounting and manifest configuration for tpt-weave"
# Publication happens in Phase 20 (todo.md); prevent accidental publishes before security/perf review.
publish = false

[features]
# Gates experimental APIs (docs/decisions.md section 6); default off.
unstable = []

[dependencies]
serde.workspace = true
toml.workspace = true

[dev-dependencies]
serde_json.workspace = true
